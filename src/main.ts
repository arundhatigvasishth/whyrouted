/**
 * whyrouted main process (M1).
 *
 * Placeholder entry point from the repo scaffold (B1). The real wiring —
 * load config, start the simulated fleet, build the registry, start the
 * health scheduler, expose GET /status — lands in J3 once the shared
 * contract (src/adapter/types.ts, src/types.ts) is in place.
 */
function main(): void {
  console.log("whyrouted: scaffold in place, main wiring not implemented yet (see J3)");
}

main();
